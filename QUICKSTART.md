# 快速使用指南

## 第一次使用

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 启动程序
双击 `run.bat` 或运行：
```bash
python uart_assistant.py
```

## 基本操作流程

### 步骤 1: 连接设备
1. 使用 USB 转串口线连接设备到电脑
2. 在"端口"下拉框中选择正确的 COM 口（如 COM1、COM3 等）
3. 设置正确的波特率（常见：9600、115200）
4. 点击"打开串口"按钮
5. 状态指示灯变绿表示连接成功

### 步骤 2: 接收数据
- 左侧窗口会实时显示接收到的数据
- 可切换"文本"或"HEX"模式查看
- 勾选"时间戳"可查看每条数据的接收时间
- 点击"暂停接收"可暂停显示（数据仍在后台接收）

### 步骤 3: 发送数据
1. 在右侧发送输入框中输入要发送的数据
2. 选择"文本"或"HEX"模式
3. 点击"发送"按钮或按 Ctrl+S

### 步骤 4: 保存数据
- 点击"保存到文件"可将接收区内容保存为.txt 或.log 文件

## 高级功能

### 批量发送测试数据
1. 在发送框输入要发送的数据
2. 勾选"启用批量发送"
3. 设置发送间隔（如 1000ms）
4. 设置发送次数（或勾选"无限循环"）
5. 点击"开始批量发送"

### 配置自动应答

#### 场景示例 1: 设备心跳应答
设备每秒发送 `AA 01`，需要自动回复 `BB 00`

配置步骤：
1. 勾选"启用自动应答"
2. 点击"配置规则"
3. 点击"添加规则"
4. 规则名称：心跳应答
5. 匹配帧配置：
   - 添加单元 1: 名称=帧头，长度=1, 类型=固定值，值=AA
   - 添加单元 2: 名称=命令，长度=1, 类型=固定值，值=01
6. 应答帧配置：
   - 添加单元 1: 名称=响应，长度=1, 类型=固定值，值=BB
   - 添加单元 2: 名称=状态，长度=1, 类型=固定值，值=00
7. 应答延迟：50ms
8. 点击"确定"保存

#### 场景示例 2: 读取设备地址
设备发送 `AA ?? 01`（??是地址），需要回复相同地址

配置步骤：
1. 匹配帧配置：
   - 单元 1: 帧头=AA（固定值）
   - 单元 2: 地址=??（通配符，长度 1）
   - 单元 3: 命令=01（固定值）
2. 应答帧配置：
   - 单元 1: 响应头=BB
   - 单元 2: 地址=从匹配帧提取（需要手动实现提取逻辑）
   - 单元 3: 状态=00

### 导入/导出规则
- **导出规则**: 点击"导出规则"，保存为 JSON 文件，可分享给他人
- **导入规则**: 点击"导入规则"，选择之前导出的 JSON 文件

## 快捷键速查

| 快捷键 | 功能说明 |
|--------|----------|
| Ctrl+O | 打开/关闭串口 |
| Ctrl+S | 立即发送数据 |
| Ctrl+C | 清空接收区 |
| Ctrl++ | 放大字体 |
| Ctrl+- | 缩小字体 |

## 常见问题排查

### 问题 1: 找不到 COM 口
**解决方法**:
1. 检查设备是否已连接
2. 检查驱动是否已安装
3. 点击"刷新"按钮重新扫描

### 问题 2: 打开串口失败
**可能原因**:
- 串口被其他程序占用
- 权限不足
- 串口号不正确

**解决方法**:
1. 关闭其他可能使用串口的程序
2. 以管理员身份运行
3. 重新选择正确的 COM 口

### 问题 3: 接收乱码
**可能原因**:
- 波特率设置错误
- 数据位/停止位/校验位不匹配

**解决方法**:
1. 确认设备的串口参数
2. 调整波特率等设置直到匹配

### 问题 4: 自动应答不工作
**检查项**:
1. ✓ 勾选了"启用自动应答"
2. ✓ 规则列表中规则已启用（复选框勾选）
3. ✓ 匹配帧配置正确
4. ✓ 接收的数据格式与匹配帧一致

## 技巧提示

1. **快速重新发送**: 发送历史会自动保存最近 20 条数据（未来版本支持）
2. **实时监控速度**: 状态栏会显示当前的接收/发送速率
3. **多窗口调试**: 可以打开多个实例同时调试不同串口
4. **规则备份**: 定期导出规则文件备份
5. **HEX 输入**: HEX 模式下输入空格会被自动忽略，如 "01 02 03" 和 "010203" 等效

## 技术支持

如有问题，请检查：
1. README.md 文档
2. 程序日志（控制台输出）
3. 配置文件位置：`C:\Users\<用户名>\.uart_tool\`
